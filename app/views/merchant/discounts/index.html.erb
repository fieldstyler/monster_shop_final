<% @merchant.discounts.each do |discount| %>
  <div id="discount-<%= discount.id %>">
  <p><%= link_to discount.name, "/merchant/discounts/#{discount.id}" %> - <%= button_to "Update Discount", "/merchant/discounts/#{discount.id}/edit", method: :get %> - <%= button_to "Remove Discount", "/merchant/discounts/#{discount.id}", method: :delete %></p>
  </div>
  <br>
<% end %>
<br>
<%= button_to "Create Discount", "/merchant/discounts/new", method: :create %>
